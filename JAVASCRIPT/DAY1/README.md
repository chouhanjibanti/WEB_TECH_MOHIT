JS -> javascript

HTML5 -> semantic , audio and video tag , svg 
CSS3 -> 
JS ES6 -> let /const , Arrow functions , promises , Async and await , template literal 

we can write JS in two ways :- 
1. internal  -> style css -> body - script 
2. External -> other file 
 
 ================================

we can run the js code in two ways :- 
1. inside the browswer
2. outside the browser :- 
    nodejs is responsible for the run js code outside the browser.


==============================================

Variable in JS :- 


Variable :- varibale is a container used to store the value.

Mainly we have three types of variable :- 
1. var 
2. let -> ES6
3. const -> ES6 


variable Declaration 
Variable initilzation/assignment 

var a;
var a = "banana"


// Diff between var , let and const.
// 1. var scope is functional scope .
// 1. let scope is block scope.
// 1. const scope is block scope.

// 2. In var declaration is poss.
// 2. In let declaration is poss.
// 2. In const declaration is not poss.

// 3. In var Reinit is poss.
// 3. In let Reinit is poss.
// 3. In const reinit is not poss.

// 4. In var Redecla is poss.
// 4. In let redecla is not poss.
// 4. In const redecla is not poss.


DataType :- Types of DataType 